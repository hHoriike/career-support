import React from "react";
import RegisterTemplate from "domains/register/template";

type Props = {};

const Register = (props: Props) => {
  return (
    <RegisterTemplate/>
  );
};

export default Register;
